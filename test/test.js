var expect    = require("chai").expect;
var submission = require("../src/responseSubmissionService");

/*
var assert = require('assert');
describe('Array', function () {
  describe('#indexOf()', function () {
    it('should return -1 when the value is not present', function () {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});
*/

describe("Response Submission Service", function() {
    describe("Payload to SQL Insert conversion", function() {
        it("converts payload data",function(){
            var regId = payload.registrationId
        });
    });
});